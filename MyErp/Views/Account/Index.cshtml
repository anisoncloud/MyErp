@using Microsoft.AspNetCore.Identity;
@model IEnumerable<Users>

<table class="table table-striped">
    <thead>
        <tr>
            <th scope="col">#</th>
            <th scope="col">Name</th>
            <th scope="col">Email</th>
            <th scope="col">Role</th>
            <th scope="col">Action</th>
        </tr>
    </thead>
    <tbody>
        @foreach(var user in Model)
        {
            <tr>
                <th scope="row">@user.Id</th>
                <td>@user.FullName</td>
                <td>@user.Email</td>
                <td></td>
                <td>
                    
                        <ul class="navbar-nav ms-auto align-items-center">
                            <li class="nav-item dropdown">
                                <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                                Actions </a>
                                <ul class="dropdown-menu dropdown-menu-end">
                                    <li><a asp-action="Edit" asp-controller="AccountDetails" asp-route-userId="@user.Id" class="dropdown-item">Edit</a></li>
                                    <li><a asp-action="AssignRole" asp-controller="Role" asp-route-id="@user.Id" class="dropdown-item">Assign Role</a></li>
                                    <li>
                                        <form asp-action="DeleteUser" asp-route-id="@user.Id" method="post" class="d-inline-block">
                                            <button type="submit" class="btn btn-danger">Delete</button>
                                        </form>
                                    </li>
                                </ul>
                            </li>
                        </ul>
                   
                    
                    
                    
                </td>
            </tr>
        }
    </tbody>
</table>